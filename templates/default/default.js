// Template JS runs after the page is decorated (progressive enhancement).
// Export a default function; it's awaited by scripts.js loadTemplateScript().
// Leave it as a no-op if this template only needs CSS.
export default async function init() {
  // Add template-specific behavior here.
}
